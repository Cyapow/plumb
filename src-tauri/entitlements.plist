<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Plumb loads libraries (e.g. Homebrew's OpenSSL) that aren't signed with
       our identity. Once the app is signed with a real identity, macOS enforces
       Library Validation and would refuse to load them, so we opt out. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <!-- WKWebView needs JIT / writable-executable memory. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
</dict>
</plist>
